apiVersion: v1
kind: Secret
metadata:
    name: harbor-cnpg-user-secret
    annotations:
        replicator.v1.mittwald.de/replicate-to: monitoring-system,cnpg-system,automation,flux-system
type: kubernetes.io/basic-auth
stringData:
    username: ${SECRET_HARBOR_PG_USER}
    password: ${SECRET_HARBOR_PG_PASSWORD}
