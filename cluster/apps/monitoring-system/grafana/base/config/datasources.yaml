# Configuration file version
apiVersion: 1

# List of data sources to delete from the database.
#deleteDatasources:
#  - name: Graphite
#    orgId: 1

# Mark provisioned data sources for deletion if they are no longer in a provisioning file.
# prune: true

# List of data sources to insert/update depending on what's available in the database.
datasources:
- name: Loki
  type: loki
  url: http://loki.monitoring-system.svc.cluster.local:3100
- name: Prometheus
  type: prometheus
  url: http://prometheus-stack-kube-prom-prometheus.monitoring-system.svc.cluster.local:8080
