# infrastructure/production/grafana-loki/kustomization.yaml
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: logging
resources:
- ../../base/grafana-loki
patches:
  - path: release-patch.yaml
    target:
      kind: HelmRelease
